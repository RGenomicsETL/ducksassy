[package]
name = "sassy-c"
version = "0.1.0"
edition = "2021"
rust-version = "1.91"
license = "MIT"
publish = false

[lib]
name = "sassy_c"
crate-type = ["staticlib", "rlib"]

[dependencies]
sassy = { version = "=0.2.6", default-features = false }

[features]
default = ["backend-scalar"]
backend-scalar = ["sassy/scalar"]
backend-avx2 = []
backend-avx512 = []
backend-neon = []

[profile.release]
lto = false
codegen-units = 1
panic = "unwind"

[workspace]

Patches applied by `bootstrap.R` after the canonical sources are copied, in
file-name order, with `patch -p1` from the package root. Use them only for
package-specific changes that R CMD check requires; fix everything else in the
canonical sources. Name them `NNN-description.patch`.
